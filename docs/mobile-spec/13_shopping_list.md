# #13 買い物リスト モバイル仕様

## 1. 概要

世帯の買い物アイテムを管理する画面。未購入・かご・購入済みの3ステータスでアイテムを管理する。

モバイル版ではPC版の2カラムレイアウトをタブ切り替えに変更する。

---

## 2. 画面構成

```
┌─────────────────────────────────────┐
│ 買い物リスト                  🔔 👤 │
├─────────────────────────────────────┤
│           [+ アイテムを追加]          │
├─────────────────────────────────────┤
│ [未購入 17件] [かご 0件] [購入済み]  │  ← タブ
├─────────────────────────────────────┤
│                                      │
│  （選択中タブのコンテンツ）           │
│                                      │
├─────────────────────────────────────┤
│  自宅 ▼  （複数世帯所属時のみ表示）  │
├─────────────────────────────────────┤
│ 🏠    📋    ✅    🛒    ⚙️          │
└─────────────────────────────────────┘
```

---

## 3. 機能仕様

### 3.1 タブ

| タブ | 表示内容 |
|---|---|
| 未購入 | 未購入アイテム一覧 + 購入場所フィルタ |
| かご | かごアイテム一覧 + 購入済みにするボタン |
| 購入済み | 直近7日間の購入済みアイテム（日付グループ） |

タブヘッダーに件数バッジを表示する。デフォルトは「未購入」タブ。

### 3.2 未購入タブ

**購入場所フィルタ**
- すべて / スーパー / ドラッグストア / オンライン
- 世帯に登録されている購入場所を動的に表示
- フロント側で絞り込み

**アイテムカード**

```
┌─────────────────────────────────┐
│ ▌ オリーブオイル          ☆  📷 │  ← ▌は購入場所カラー
│   メモ（任意）                   │
│                        [かごへ] │
└─────────────────────────────────┘
```

| 要素 | 内容 |
|---|---|
| 購入場所カラーバー | カード左端に購入場所に対応した色のバーを表示 |
| 品名 | |
| メモ | 登録されている場合のみ表示 |
| お気に入りアイコン | ☆/★ タップでお気に入りトグル |
| 画像アイコン | 📷 画像が添付されている場合のみ表示。タップで#15詳細画面へ遷移 |
| かごへボタン | タップでステータスを「かご」に更新 |

カードタップで#15買い物アイテム詳細画面へ遷移。

### 3.3 かごタブ

**購入済みにするボタン（ヘッダー）**
- かご内の全アイテムを一括で購入済みに更新
- 会計完了後にまとめて更新するユースケースを想定
- タップで確認ダイアログを表示

**アイテムカード**

```
┌─────────────────────────────────┐
│ ▌ オリーブオイル          ☆  📷 │
│   メモ（任意）                   │
│               [戻す] [購入済み] │
└─────────────────────────────────┘
```

| 操作 | 内容 |
|---|---|
| 戻すボタン | ステータスを「未購入」に戻す |
| 購入済みボタン | そのアイテムのみステータスを「購入済み」に更新 |

> 💡 想定ユースケース: 商品を手に取るたびに「かごへ」→ 会計後に一括「購入済みにする」が基本操作。カードごとの「購入済み」は個別に完了したいときに使用。

### 3.4 購入済みタブ

- 直近7日間の購入済みアイテムを日付グループで表示
- 各グループ: 日付 + 件数
- アイテム行: 品名 + 購入場所バッジ
- アイテム行タップで#15買い物アイテム詳細画面へ遷移

---

## 4. API

既存APIをそのまま利用する。

| メソッド | エンドポイント | 用途 |
|---|---|---|
| GET | `/api/households/{householdId}/shopping-items` | アイテム全件取得。フィルタはフロント側で実施 |
| PATCH | `/api/shopping-items/{shoppingItemId}/status` | ステータス更新（かごへ / 戻す / 購入済み） |
| PATCH | `/api/shopping-items/bulk-status` | かご内アイテムの一括購入済み更新 |
|PATCH | `/api/shopping-items/{id}/favorite` | お気に入りの切り替え |

---

## 5. 状態管理

```
ShoppingListState
├── items: List<ShoppingItem>       // 全アイテム（フィルタ前）
├── locationFilter: String          // 購入場所フィルタ（未購入タブ用）
├── activeTab: ShoppingTab          // unpurchased / cart / purchased
└── isLoading: bool
```

---

## 6. 世帯切り替え

→ [共通仕様: 世帯切り替え（HouseholdSwitcher）](./common/household_switcher.md) 参照

---

*v0.1 / 2026-04-27 時点*
